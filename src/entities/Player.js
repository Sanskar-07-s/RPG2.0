/**
 * Player Entity class.
 * Handles the camera rig, movement vectors, state, and interaction.
 */
export class Player {
  constructor(scene) {
    this.scene = scene;
    console.log("Player entity stub initialized");
  }

  update() {
    // Will be populated in Step 2 with movement mechanics
  }
}
