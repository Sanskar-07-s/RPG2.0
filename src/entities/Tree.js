/**
 * Reusable Tree object entity.
 * Creates procedural tree meshes for obstacle placements.
 */
export class Tree {
  constructor(scene, position) {
    this.scene = scene;
    console.log("Tree obstacle stub initialized");
  }
}
