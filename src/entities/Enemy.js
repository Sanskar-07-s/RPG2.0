/**
 * Reusable Enemy entity.
 * Handles AI navigation/patrol pathing.
 */
export class Enemy {
  constructor(scene, position) {
    this.scene = scene;
    console.log("Enemy target stub initialized");
  }

  update() {
    // Will be populated in Step 4 with movement AI
  }
}
