/**
 * WeaponSystem class.
 * Handles shot raycasting, trigger presses, ammo updates, and hit registration.
 */
export class WeaponSystem {
  constructor(scene) {
    this.scene = scene;
    console.log("WeaponSystem stub initialized");
  }

  update() {
    // Will be populated in Step 3
  }
}
