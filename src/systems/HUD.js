/**
 * HUD class.
 * Updates overlay items (health bar, ammo gauges, score counters).
 */
export class HUD {
  constructor() {
    console.log("HUD stub initialized");
  }

  update() {
    // Will be populated in Step 4
  }
}
